# Docushield-AI-service
DocuShield AI microservice — Python/FastAPI service for contract extraction, embedding, and risk classification via RAG. Consumes jobs from the Gateway's Redis queue; never called directly.
